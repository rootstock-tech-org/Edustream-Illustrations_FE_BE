# AI tutor provider. Without this key the app uses a deterministic, grounded
# offline fallback (no hallucinated physics). With it, the tutor uses
# Groq-hosted gpt-oss-120b via the OpenAI-compatible API.
GROQ_API_KEY=
